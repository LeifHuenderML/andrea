{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "CUDA C++: Debug",
            "type": "cuda-gdb",
            "request": "launch",
            "program": "${workspaceFolder}/build/main",
            "args": [],
            "stopAtEntry": true,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "preLaunchTask": "build"
        }
    ]
}